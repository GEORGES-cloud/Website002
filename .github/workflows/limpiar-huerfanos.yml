name: Limpiar archivos huérfanos del servidor

# Borra por FTP archivos que ya no forman parte del sitio (imágenes y vídeos
# archivados) y que el sync incremental no elimina. Manual (workflow_dispatch),
# de un solo uso. No toca ningún archivo en uso.

on:
  workflow_dispatch:

jobs:
  limpiar:
    runs-on: ubuntu-latest
    steps:
      - name: Borrar huérfanos por FTP
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          npm install basic-ftp@5 --no-fund --no-audit --silent
          cat > limpiar.mjs <<'EOF'
          import * as ftp from 'basic-ftp';
          const objetivos = [
            'public_html/assets/video/hero.mp4',
            'public_html/assets/video/cocina.mp4',
            'public_html/assets/video/hero-montaje.mp4',
            'public_html/assets/video/hero-old.mp4',
            'public_html/assets/img/real2-tartar.jpg',
            'public_html/assets/img/real2-tartar.webp',
            'public_html/assets/img/real2-carabineros.jpg',
            'public_html/assets/img/real2-carabineros.webp',
            'public_html/assets/img/real2-carne.jpg',
            'public_html/assets/img/real2-carne.webp',
            'public_html/assets/img/real2-jardin.jpg',
            'public_html/assets/img/real2-jardin.webp',
            'public_html/assets/img/real2-postre.jpg',
            'public_html/assets/img/real2-postre.webp',
            'public_html/assets/img/logo-zenorio.png',
            'public_html/assets/img/logo-wordmark.png',
            'public_html/assets/img/logo-zenorio-word.svg',
            'public_html/assets/img/logo-z.png',
            'public_html/assets/img/hero-poster.jpg',
          ];
          const c = new ftp.Client(30000);
          try {
            await c.access({ host: process.env.FTP_SERVER, user: process.env.FTP_USERNAME, password: process.env.FTP_PASSWORD, secure: true, secureOptions: { rejectUnauthorized: false } });
            for (const p of objetivos) {
              try { await c.remove(p); console.log('BORRADO  ' + p); }
              catch (e) { console.log('(ausente) ' + p + '  [' + (e.code || e.message.split('\n')[0]) + ']'); }
            }
            try { await c.removeDir('public_html/assets/video'); console.log('BORRADA carpeta assets/video'); }
            catch (e) { console.log('(carpeta video no vaciable/ausente) [' + (e.code || 'x') + ']'); }
          } finally { c.close(); }
          EOF
          node limpiar.mjs
